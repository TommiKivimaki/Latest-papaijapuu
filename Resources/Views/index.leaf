#set("content") {

<div class="indexContentArea">
  
  #if(messages) {
  <ul>
    #for(message in messages) {
    <li>#(message.message)</li>
    }
  </ul>
  
  } else {
  <!-- Nothing to display here -->
  }
  
</div>

} 
#embed("base")

