#set("content") {
<h1>Index -> Style this page to be embedded in an iFrame</h1>

#if(messages) {
<ul>
  #for(message in messages) {
  <li>#(message.message)</li>
  }
</ul>

} else {
<!-- Nothing to display here -->
}


} 
#embed("base")

